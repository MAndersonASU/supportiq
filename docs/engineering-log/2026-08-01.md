# 2026-08-01 — Day 1: Project setup

## Goal

Stand up the repository and project skeleton before any pipeline work
begins.

## Decisions

- **Project**: SupportIQ — a customer support ticket platform combining a
  data pipeline, an ML triage classifier, and a Claude-powered RAG
  resolution assistant.
- **Scope**: local-first development; a cloud deploy is planned only at the
  end, for the demo.
- **LLM provider**: Claude API, for the retrieval-augmented generation
  layer in Phase 3.
- **Repo**: [MAndersonASU/supportiq](https://github.com/MAndersonASU/supportiq),
  public, MIT licensed.

## What was done

- Installed and authenticated the GitHub CLI.
- Created the GitHub repository and initialized the local git repo.
- Scaffolded the project layout: `src/{data,features,models,ai,serving}`,
  `tests/`, `docs/engineering-log/`.
- Wrote the initial `README.md` and `docs/architecture.md` describing the
  five-stage pipeline (ingestion → features → ML model → RAG → serving).
- Added `.gitignore` (excludes raw/processed data, model artifacts, MLflow
  run directories — data will be tracked separately via DVC once the
  ingestion pipeline exists).

## Next session (Day 2)

Finalize the ticket dataset choice and build the ingestion pipeline stage
(`src/data`): load raw tickets into a defined schema and land them as the
first artifact in `data/raw`.
